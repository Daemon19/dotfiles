- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.aliases: aliases
    ~/.vimrc: vimrc
    ~/.config/nvim/init.vim: init.vim
    ~/.p10k.zsh: p10k.zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
